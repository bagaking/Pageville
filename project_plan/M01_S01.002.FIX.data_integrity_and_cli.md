# M01_S01.002.FIX — 数据完整性与 CLI 契约

> meta：记录第二轮优化（删重复 + 找真 bug）。
> 触发：用户 `优化轮吧`（ponytail full）。
> 状态：**已完成并验收**。

## 背景

第一轮全绿之后的复查。结论是**绿色不等于没问题**：静态读代码没发现问题，
是跑畸形输入 fuzz 和故障注入才撞出来的。

## OKR

- **O**：消除"静默给出错误答案"这一类缺陷。
- **KR1**：垃圾时间参数不再静默过滤 —— 达成（400）。
- **KR2**：CLI 任何失败都非零退出 —— 达成。
- **KR3**：损坏的 CAS 对象能自愈 —— 达成，且经 mutation 验证。

## Checklist

- [x] **时间参数校验前移**：`since`/`until` 无法解析时返回 400，删掉比较函数里
      "解析失败回退字符串比较"的兜底（O-003）
- [x] **`cmd_events` 补错误检查**：曾把 400 响应体当事件流打印并 exit 0（O-004）
- [x] **CAS 截断自愈**：`exists()` 改为长度比对 + `write tmp` + `rename`（O-005）
- [x] 两条 CLI GET 路径收敛为 `get_text()`，保留服务端 `error` 详情；
      `futuresless_get`（早期残留命名）改名 `print_list`
- [x] 清理叠了两层、前半段描述已删逻辑的 stale 文档注释
- [x] README 失真修正：已删除的 `manifests/` 目录、1.5MiB 发布上限、
      三道安全防线的**原因**、符号链接跳过、`--target` 语义、切片数 7→11、许可证

## 量化验收标准

| 指标 | 目标 | 实测 |
| --- | --- | --- |
| 畸形输入 fuzz（13 例：路径穿越/超长/空字节/畸形 JSON） | 无 500、无 panic、daemon 存活 | 全部 400/404/422，daemon 存活 |
| 生产路径 `unwrap` 可被外部输入触发 | 0 | 0（16 处逐个核查） |
| `since=notatime` | 400 且 CLI 非零退出 | 400，exit 1，stdout 为空 |
| 截断的 CAS 对象重新发布后 | 恢复完整长度 | 恢复（210/210） |
| **mutation 验证**：改回旧 `ensure_object` 后 `t002` | 必须失败 | `FAIL: truncated object not repaired`，exit 1 |
| 验收切片 | 12 个全通过 | 12/12 |

## 偏差记录

**本轮最重要的一条**：上一轮修字符串比较时，我自己留下的"解析失败兜底"制造了
O-003 那个静默错误答案。连续两轮都在自己的修复里发现新缺陷 —— 这直接催生了
O-002（反向验证）作为固定动作。
